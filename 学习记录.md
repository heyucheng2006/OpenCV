# OpenCV 学习记录

---

## 2025-07-04 — 环境搭建

- [x] 安装 Anaconda（F:\anaconda）
- [x] 创建 OpenCV 专用环境（Python 3.11.15）
- [x] 安装 OpenCV 5.0.0 + NumPy 2.4.6
- [x] 配置 VS Code（解释器 + 调试器）
- [x] 第一个 Demo 运行成功（demo.py）

---

## 教材进度

| 章节 | 内容 | 状态 |
|------|------|------|
| 第 1 章 | OpenCV 入门 | ✅ 完成 |
| 第 2 章 | 图像处理基础 | ✅ 完成 |
| 第 3 章 | 图像运算 | ✅ 完成 |
| 第 4 章 | 色彩空间类型转换 | ⬜ |
| 第 5 章 | 几何变换 | ⬜ |
| 第 6 章 | 阈值处理 | ⬜ |
| 第 7 章 | 图像平滑处理 | ⬜ |
| 第 8 章 | 形态学操作 | ⬜ |
| 第 9 章 | 图像梯度 | ⬜ |
| 第 10 章 | Canny 边缘检测 | ⬜ |
| 第 11 章 | 图像金字塔 | ⬜ |
| 第 12 章 | 图像轮廓 | ⬜ |
| 第 13 章 | 直方图处理 | ⬜ |
| 第 14 章 | 傅里叶变换 | ⬜ |
| 第 15 章 | 模板匹配 | ⬜ |
| 第 16 章 | 霍夫变换 | ⬜ |
| 第 17 章 | 图像分割与提取 | ⬜ |
| 第 18 章 | 视频处理 | ⬜ |
| 第 19 章 | 绘图及交互 | ⬜ |
| 第 20 章 | K 近邻算法 | ⬜ |
| 第 21 章 | 支持向量机 | ⬜ |
| 第 22 章 | K 均值聚类 | ⬜ |
| 第 23 章 | 人脸识别 | ⬜ |

### 第 1 章 OpenCV 入门

- [x] 1.1 如何使用
- [x] 1.2 图像处理基本操作
- [x] 1.3 OpenCV 贡献库

本章主要学习了 OpenCV 的基本使用方式，包括环境运行、图像读取、显示、保存，以及主库和贡献库的区别。常用函数包括 `cv2.imread()`、`cv2.imshow()`、`cv2.waitKey()`、`cv2.destroyAllWindows()` 和 `cv2.imwrite()`。

详细环境说明和常用命令见 [README.md](README.md)；示例代码见 [demo.py](demo.py)；VS Code 解释器配置见 [.vscode/settings.json](.vscode/settings.json)。

---

### 第 2 章 图像处理基础

- [x] 2.1 图像的基本表示方法
- [x] 2.2 像素处理
- [x] 2.3 使用 `numpy.array` 访问像素
- [x] 2.4 感兴趣区域 ROI
- [x] 2.5 通道操作
- [x] 2.6 获取图像属性

本章主要学习了 OpenCV 图像数据的底层表示方式：图像本质上是 NumPy 数组。灰度图像是二维数组，彩色图像是三维数组；OpenCV 中彩色图像默认通道顺序是 `BGR`，不是 `RGB`。

重点知识：

- 二值图像可以理解为特殊的灰度图像，常用 `0` 表示黑色，`255` 表示白色。
- 灰度图像通过 `img[row, col]` 访问像素，彩色图像通过 `img[row, col, channel]` 访问某个通道。
- 彩色图像通道含义：`0` 是 B 通道，`1` 是 G 通道，`2` 是 R 通道。
- ROI 本质是数组切片，例如 `img[y1:y2, x1:x2]`，注意顺序是先行后列，也就是先 `y` 后 `x`。
- 通道可以用索引拆分，也可以用 `cv2.split()` 拆分，用 `cv2.merge()` 合并。
- 常用图像属性包括 `shape`、`size`、`dtype`，分别用于查看图像形状、数组元素总数和像素数据类型。

常用代码形式：

```python
img_gray[row, col]              # 访问灰度图像像素
img_color[row, col, channel]    # 访问彩色图像某个通道
roi = img[y1:y2, x1:x2]         # 获取感兴趣区域
b, g, r = cv2.split(img)        # 拆分通道
img2 = cv2.merge([b, g, r])     # 合并通道
print(img.shape, img.size, img.dtype)
```

图像显示固定三段式：

```python
cv2.imshow("窗口标题", img)
cv2.waitKey(0)
cv2.destroyAllWindows()
```

学习提醒：

- 教材中的 `itemset()` 在新版 NumPy 中可能不可用，实际练习时优先使用数组索引赋值，例如 `img[3, 2] = 255`。
- 如果 `cv2.imread()` 路径写错，会返回 `None`，查看属性前可以先判断 `img is None`。
- OpenCV 图像坐标和数组索引要区分：数组访问是 `img[y, x]`，其中 `y` 是行，`x` 是列。
- `uint8` 的取值范围是 `0~255`，直接用 NumPy 做加减法时要注意溢出问题。
- ROI 切片通常是原图的视图，修改 ROI 可能会同步修改原图；如果需要独立副本，应使用 `.copy()`。
- 单独显示某个通道时，显示结果是灰度图，亮的地方表示该通道分量大，并不是直接显示成蓝色、绿色或红色。
- OpenCV 的 `cv2.imshow()` 按 BGR 正常显示；如果用 Matplotlib 显示 OpenCV 读取的彩色图，通常需要先转换为 RGB。
- `np.random.randint(0, 256, size)` 的 `size` 直接写成形状元组，例如 `(180, 100, 3)`，不要多包一层或把范围参数写进形状里。
- 通道清零是累积修改；如果要分别观察不同通道清零效果，应重新读取图像或使用 `.copy()` 创建副本。
- 脚本放在子目录运行时要注意图片路径，必要时使用 `../001.bmp` 访问上级目录中的图片。

示例代码已整理在 [第二章代码](第二章代码) 目录，包括例 2.1 到例 2.19；代码复习笔记见 [第二章代码总结.md](第二章代码/第二章代码总结.md)。

---

### 第 3 章 图像运算

- [x] 3.1 图像加法运算
- [x] 3.2 图像加权和
- [x] 3.3 按位逻辑运算
- [x] 3.4 掩模
- [x] 3.5 图像与数值的运算
- [x] 3.6 位平面分解
- [x] 3.7 图像加密和解密
- [x] 3.8 数字水印
- [x] 3.9 脸部打码及解码

本章主要学习了图像的算术运算和按位逻辑运算，并用这些基础操作实现了图像融合、掩模提取、位平面分解、异或加密、数字水印和局部打码还原。

重点知识：

- NumPy 的 `+` 对 `uint8` 图像做加法时会发生取模回绕，`cv2.add()` 做的是饱和加法。
- `cv2.addWeighted()` 用于图像加权融合，公式是 `dst = src1 * alpha + src2 * beta + gamma`。
- 常用位运算包括 `cv2.bitwise_and()`、`cv2.bitwise_or()`、`cv2.bitwise_not()` 和 `cv2.bitwise_xor()`。
- 掩模 `mask` 用来控制运算区域，作为函数参数时通常要求是 8 位单通道数组。
- 位平面分解通过按位与提取像素二进制位，低位影响小，高位更接近原图结构。
- 异或满足 `A xor K xor K = A`，可以用于简单图像加密和解密。
- 数字水印可以嵌入图像最低位，因为最低位对视觉影响较小。
- 第 3.7~3.9 节的关键是把异或、最低位和 mask 组合起来：异或负责可逆加密，最低位负责隐藏水印，mask 负责指定只处理局部区域。

常用代码形式：

```python
result = cv2.add(img1, img2)                    # 饱和加法
blend = cv2.addWeighted(img1, 0.6, img2, 0.4, 0) # 加权融合
mask = np.zeros(img.shape[:2], dtype=np.uint8)  # 单通道掩模
roi = cv2.bitwise_and(img, img, mask=mask)      # 用 mask 保留局部
encrypted = cv2.bitwise_xor(img, key)           # 异或加密
restored = cv2.bitwise_xor(encrypted, key)      # 异或解密
```

学习提醒：

- 图像加权融合前，两张图或两个 ROI 的大小、类型和通道数必须一致。
- `mask=` 参数不能直接使用三通道图像；三通道数组可以参与 `bitwise_and(a, b)`，但不能直接作为函数的 `mask` 参数。
- `mask` 有时用 `0/255` 方便显示，有时用 `0/1` 方便计算，使用前要确认当前语义。
- 位平面提取后要把非零值阈值化为 `255`，否则低位平面显示会非常暗。
- 异或加密必须保存或复用同一个 `key`，否则无法正确还原。
- 数字水印和脸部打码都不是新的独立算法，而是按位运算、mask 和区域拼接的组合应用；复习时优先画清楚“哪些区域被保留、哪些位被修改、用哪个 key 还原”。
- 复习 3.7~3.9 时可以按三步拆：先看“二进制位如何变化”，再看“mask 保留哪一块”，最后看“结果图由哪几部分拼回来”。

难点理解：

- 3.7 图像加密和解密：核心是异或的可逆性。`A xor K` 会用密钥 `K` 翻转原图 `A` 的部分二进制位，得到加密图；再执行一次 `(A xor K) xor K`，被翻转的位会翻转回来，所以恢复成 `A`。关键条件是解密必须使用同一个 `key`，而且 `key` 的尺寸要和原图一致。
- 3.8 数字水印：核心是最低位隐藏。8 位灰度像素的最高位影响最大，最低位影响最小；把水印压缩成 `0/1` 后嵌入载体图像的最低位，视觉上变化很小，但可以通过 `0000 0001` 这样的提取矩阵重新取出来。代码里的 `254 = 1111 1110` 用来清除最低位，`1 = 0000 0001` 用来提取最低位。
- 3.9 脸部打码及解码：核心是局部加密和区域拼接。先用 `mask` 标记要加密的区域，用 `1-mask` 标记不加密的区域；加密图像由“异或后的脸部区域 + 原始非脸部区域”组成，还原图像由“解密后的脸部区域 + 原始非脸部区域”组成。这里要特别注意 `mask` 的值有时是 `0/1` 方便计算，显示或按位与时常需要乘以 `255`。

示例代码已整理在 [第三章代码](第三章代码) 目录；代码复习笔记见 [第三章代码总结.md](第三章代码/第三章代码总结.md)。
