# OpenCV 学习记录

---

## 2025-07-04 — 环境搭建

- [x] 安装 Anaconda（F:\anaconda）
- [x] 创建 OpenCV 专用环境（Python 3.11.15）
- [x] 安装 OpenCV 5.0.0 + NumPy 2.4.6
- [x] 配置 VS Code（解释器 + 调试器）
- [x] 第一个 Demo 运行成功（demo.py）

---

## 教材进度

| 章节 | 内容 | 状态 |
|------|------|------|
| 第 1 章 | OpenCV 入门 | ✅ 完成 |
| 第 2 章 | 图像处理基础 | ✅ 完成 |
| 第 3 章 | 图像运算 | ✅ 完成 |
| 第 4 章 | 色彩空间类型转换 | ✅ 完成 |
| 第 5 章 | 几何变换 | ✅ 完成 |
| 第 6 章 | 阈值处理 | ✅ 完成 |
| 第 7 章 | 图像平滑处理 | ⬜ |
| 第 8 章 | 形态学操作 | ⬜ |
| 第 9 章 | 图像梯度 | ⬜ |
| 第 10 章 | Canny 边缘检测 | ⬜ |
| 第 11 章 | 图像金字塔 | ⬜ |
| 第 12 章 | 图像轮廓 | ⬜ |
| 第 13 章 | 直方图处理 | ⬜ |
| 第 14 章 | 傅里叶变换 | ⬜ |
| 第 15 章 | 模板匹配 | ⬜ |
| 第 16 章 | 霍夫变换 | ⬜ |
| 第 17 章 | 图像分割与提取 | ⬜ |
| 第 18 章 | 视频处理 | ⬜ |
| 第 19 章 | 绘图及交互 | ⬜ |
| 第 20 章 | K 近邻算法 | ⬜ |
| 第 21 章 | 支持向量机 | ⬜ |
| 第 22 章 | K 均值聚类 | ⬜ |
| 第 23 章 | 人脸识别 | ⬜ |

### 第 1 章 OpenCV 入门

- [x] 1.1 如何使用
- [x] 1.2 图像处理基本操作
- [x] 1.3 OpenCV 贡献库

本章主要学习了 OpenCV 的基本使用方式，包括环境运行、图像读取、显示、保存，以及主库和贡献库的区别。常用函数包括 `cv2.imread()`、`cv2.imshow()`、`cv2.waitKey()`、`cv2.destroyAllWindows()` 和 `cv2.imwrite()`。

详细环境说明和常用命令见 [README.md](README.md)；示例代码见 [demo.py](demo.py)；VS Code 解释器配置见 [.vscode/settings.json](.vscode/settings.json)。

---

### 第 2 章 图像处理基础

- [x] 2.1 图像的基本表示方法
- [x] 2.2 像素处理
- [x] 2.3 使用 `numpy.array` 访问像素
- [x] 2.4 感兴趣区域 ROI
- [x] 2.5 通道操作
- [x] 2.6 获取图像属性

本章主要学习了 OpenCV 图像数据的底层表示方式：图像本质上是 NumPy 数组。灰度图像是二维数组，彩色图像是三维数组；OpenCV 中彩色图像默认通道顺序是 `BGR`，不是 `RGB`。

重点知识：

- 二值图像可以理解为特殊的灰度图像，常用 `0` 表示黑色，`255` 表示白色。
- 灰度图像通过 `img[row, col]` 访问像素，彩色图像通过 `img[row, col, channel]` 访问某个通道。
- 彩色图像通道含义：`0` 是 B 通道，`1` 是 G 通道，`2` 是 R 通道。
- ROI 本质是数组切片，例如 `img[y1:y2, x1:x2]`，注意顺序是先行后列，也就是先 `y` 后 `x`。
- 通道可以用索引拆分，也可以用 `cv2.split()` 拆分，用 `cv2.merge()` 合并。
- 常用图像属性包括 `shape`、`size`、`dtype`，分别用于查看图像形状、数组元素总数和像素数据类型。

常用代码形式：

```python
img_gray[row, col]              # 访问灰度图像像素
img_color[row, col, channel]    # 访问彩色图像某个通道
roi = img[y1:y2, x1:x2]         # 获取感兴趣区域
b, g, r = cv2.split(img)        # 拆分通道
img2 = cv2.merge([b, g, r])     # 合并通道
print(img.shape, img.size, img.dtype)
```

图像显示固定三段式：

```python
cv2.imshow("窗口标题", img)
cv2.waitKey(0)
cv2.destroyAllWindows()
```

学习提醒：

- 教材中的 `itemset()` 在新版 NumPy 中可能不可用，实际练习时优先使用数组索引赋值，例如 `img[3, 2] = 255`。
- 如果 `cv2.imread()` 路径写错，会返回 `None`，查看属性前可以先判断 `img is None`。
- OpenCV 图像坐标和数组索引要区分：数组访问是 `img[y, x]`，其中 `y` 是行，`x` 是列。
- `uint8` 的取值范围是 `0~255`，直接用 NumPy 做加减法时要注意溢出问题。
- ROI 切片通常是原图的视图，修改 ROI 可能会同步修改原图；如果需要独立副本，应使用 `.copy()`。
- 单独显示某个通道时，显示结果是灰度图，亮的地方表示该通道分量大，并不是直接显示成蓝色、绿色或红色。
- OpenCV 的 `cv2.imshow()` 按 BGR 正常显示；如果用 Matplotlib 显示 OpenCV 读取的彩色图，通常需要先转换为 RGB。
- `np.random.randint(0, 256, size)` 的 `size` 直接写成形状元组，例如 `(180, 100, 3)`，不要多包一层或把范围参数写进形状里。
- 通道清零是累积修改；如果要分别观察不同通道清零效果，应重新读取图像或使用 `.copy()` 创建副本。
- 脚本放在子目录运行时要注意图片路径，必要时使用 `../001.bmp` 访问上级目录中的图片。

示例代码已整理在 [第二章代码](第二章代码) 目录，包括例 2.1 到例 2.19；代码复习笔记见 [第二章代码总结.md](第二章代码/第二章代码总结.md)。

---

### 第 3 章 图像运算

- [x] 3.1 图像加法运算
- [x] 3.2 图像加权和
- [x] 3.3 按位逻辑运算
- [x] 3.4 掩模
- [x] 3.5 图像与数值的运算
- [x] 3.6 位平面分解
- [x] 3.7 图像加密和解密
- [x] 3.8 数字水印
- [x] 3.9 脸部打码及解码

本章主要学习了图像的算术运算和按位逻辑运算，并用这些基础操作实现了图像融合、掩模提取、位平面分解、异或加密、数字水印和局部打码还原。

重点知识：

- NumPy 的 `+` 对 `uint8` 图像做加法时会发生取模回绕，`cv2.add()` 做的是饱和加法。
- `cv2.addWeighted()` 用于图像加权融合，公式是 `dst = src1 * alpha + src2 * beta + gamma`。
- 常用位运算包括 `cv2.bitwise_and()`、`cv2.bitwise_or()`、`cv2.bitwise_not()` 和 `cv2.bitwise_xor()`。
- 掩模 `mask` 用来控制运算区域，作为函数参数时通常要求是 8 位单通道数组。
- 位平面分解通过按位与提取像素二进制位，低位影响小，高位更接近原图结构。
- 异或满足 `A xor K xor K = A`，可以用于简单图像加密和解密。
- 数字水印可以嵌入图像最低位，因为最低位对视觉影响较小。
- 第 3.7~3.9 节的关键是把异或、最低位和 mask 组合起来：异或负责可逆加密，最低位负责隐藏水印，mask 负责指定只处理局部区域。

常用代码形式：

```python
result = cv2.add(img1, img2)                    # 饱和加法
blend = cv2.addWeighted(img1, 0.6, img2, 0.4, 0) # 加权融合
mask = np.zeros(img.shape[:2], dtype=np.uint8)  # 单通道掩模
roi = cv2.bitwise_and(img, img, mask=mask)      # 用 mask 保留局部
encrypted = cv2.bitwise_xor(img, key)           # 异或加密
restored = cv2.bitwise_xor(encrypted, key)      # 异或解密
```

学习提醒：

- 图像加权融合前，两张图或两个 ROI 的大小、类型和通道数必须一致。
- `mask=` 参数不能直接使用三通道图像；三通道数组可以参与 `bitwise_and(a, b)`，但不能直接作为函数的 `mask` 参数。
- `mask` 有时用 `0/255` 方便显示，有时用 `0/1` 方便计算，使用前要确认当前语义。
- 位平面提取后要把非零值阈值化为 `255`，否则低位平面显示会非常暗。
- 异或加密必须保存或复用同一个 `key`，否则无法正确还原。
- 数字水印和脸部打码都不是新的独立算法，而是按位运算、mask 和区域拼接的组合应用；复习时优先画清楚“哪些区域被保留、哪些位被修改、用哪个 key 还原”。
- 复习 3.7~3.9 时可以按三步拆：先看“二进制位如何变化”，再看“mask 保留哪一块”，最后看“结果图由哪几部分拼回来”。

难点理解：

- 3.7 图像加密和解密：核心是异或的可逆性。`A xor K` 会用密钥 `K` 翻转原图 `A` 的部分二进制位，得到加密图；再执行一次 `(A xor K) xor K`，被翻转的位会翻转回来，所以恢复成 `A`。关键条件是解密必须使用同一个 `key`，而且 `key` 的尺寸要和原图一致。
- 3.8 数字水印：核心是最低位隐藏。8 位灰度像素的最高位影响最大，最低位影响最小；把水印压缩成 `0/1` 后嵌入载体图像的最低位，视觉上变化很小，但可以通过 `0000 0001` 这样的提取矩阵重新取出来。代码里的 `254 = 1111 1110` 用来清除最低位，`1 = 0000 0001` 用来提取最低位。
- 3.9 脸部打码及解码：核心是局部加密和区域拼接。先用 `mask` 标记要加密的区域，用 `1-mask` 标记不加密的区域；加密图像由“异或后的脸部区域 + 原始非脸部区域”组成，还原图像由“解密后的脸部区域 + 原始非脸部区域”组成。这里要特别注意 `mask` 的值有时是 `0/1` 方便计算，显示或按位与时常需要乘以 `255`。

示例代码已整理在 [第三章代码](第三章代码) 目录；代码复习笔记见 [第三章代码总结.md](第三章代码/第三章代码总结.md)。

---

### 第 4 章 色彩空间类型转换

- [x] 4.1 色彩空间基础
- [x] 4.2 类型转换函数
- [x] 4.3 类型转换实例
- [x] 4.4 HSV 色彩空间讨论
- [x] 4.5 alpha 通道

本章主要学习了 OpenCV 中不同色彩空间的含义和转换方法。核心函数是 `cv2.cvtColor()`，常用转换包括 `BGR -> GRAY`、`GRAY -> BGR`、`BGR <-> RGB`、`BGR <-> HSV` 和 `BGR -> BGRA`。

重点知识：

- OpenCV 读取彩色图像后默认是 `BGR` 顺序，不是传统的 `RGB`。
- BGR 转灰度公式（ITU-R BT.601）：`Gray = 0.114B + 0.587G + 0.299R`。系数反映人眼对不同光的敏感度（绿最敏感 0.587，蓝最不敏感 0.114），三系数和为 1.0。
- `cv2.COLOR_GRAY2BGR` 不能恢复彩色，只是把灰度值复制到 B=G=R，没有任何加权运算。
- BGR ↔ RGB 本质是交换第 0 通道和第 2 通道，不涉及数学运算。
- BGR → GRAY 是有损转换，颜色信息永久丢失；GRAY → BGR 只是格式上的三通道包装，B=G=R 始终相等。
- HSV 更适合按颜色筛选目标：`H` 色调（0~180，实际角度÷2）、`S` 饱和度（0~255）、`V` 明度（0~255）。
- HSV 色调在色环上的分布：纯红 H=0，纯绿 H=60，纯蓝 H=120。
- HSV 三通道可分别索引取出：`H=hsv[:,:,0]`、`S=hsv[:,:,1]`、`V=hsv[:,:,2]`。
- `cv2.inRange()` 生成二值掩码：范围内为 255（白），范围外为 0（黑）。
- `cv2.bitwise_and(img, img, mask=mask)` 根据掩码保留区域，其余区域变黑（非目标区域的原始颜色不显示）。
- `H` 和 `S` 的掩码可以用 `&`（按位与）合并，两个条件同时满足才保留。
- V 通道控制亮度，固定 V 值可产生艺术化效果；修改 HSV 后需转回 BGR 才能用 imshow 显示。
- alpha 通道表示透明度：0 全透明、125 半透明、255 全不透明（默认值）。默认 `BGR2BGRA` 追加的是 255。
- `cv2.imshow()` 看不出 alpha 透明效果，保存为 PNG 后用支持透明的查看器才能观察。

常用代码形式：

```python
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
bgr = cv2.cvtColor(gray, cv2.COLOR_GRAY2BGR)
rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)
bgra = cv2.cvtColor(img, cv2.COLOR_BGR2BGRA)

H, S, V = hsv[:,:,0], hsv[:,:,1], hsv[:,:,2]

mask = cv2.inRange(hsv, lower, upper)
mask_combined = hueMask & satMask
roi = cv2.bitwise_and(img, img, mask=mask)

b, g, r, a = cv2.split(bgra)
a[:, :] = 125
bgra125 = cv2.merge([b, g, r, a])
```

学习提醒：

- 颜色提取时不要直接在 BGR 空间硬凑范围，通常先转 HSV 再用 `inRange()`。
- 红色在 HSV 色调环两端（H≈0 和 H≈180），实际项目中常需两个范围合并提取。
- 饱和度和亮度太低时颜色的 H 值不稳定，提取颜色时通常给 S、V 设下限排除干扰。
- 使用 `cv2.imshow()` 显示 RGB 图像时颜色会反，因为 `imshow()` 按 BGR 解释。
- `mask` 参数必须是单通道 8 位数组；bitwise_and 的两个输入图可以是三通道。
- 子目录里的脚本注意图片路径。
- `cv2.imread()` 读图失败返回 `None`，实际代码中最好先判断。
- 常见拼写错误：`cvtColer` → `cvtColor`，`size[5,5]` → `size=[5,5]`。

示例代码已整理在 [第四章代码](第四章代码) 目录，包括例 4.1 到例 4.13；代码复习笔记见 [第四章代码总结.md](第四章代码/第四章代码总结.md)。

---

### 第 5 章 几何变换

- [x] 5.1 缩放
- [x] 5.2 翻转
- [x] 5.3 仿射
- [x] 5.4 透视
- [x] 5.5 重映射

本章主要学习了 OpenCV 中几何变换的常用方法。几何变换的核心不是改变像素颜色，而是改变像素位置或重新定义目标图像中每个像素来自原图哪里。本章可以按三个层次理解：**手动写矩阵 → 点对算矩阵 → 像素级控制**。

重点知识：

- `cv2.resize()` 用于缩放图像，可以通过 `dsize` 指定目标大小，也可以通过 `fx`、`fy` 指定缩放比例。常用插值：`INTER_AREA`（缩小）、`INTER_LINEAR`（默认）、`INTER_CUBIC`（放大）。
- OpenCV 图像的 `shape` 顺序是 `(height, width)`，而 `resize()`、`warpAffine()`、`warpPerspective()` 中的 `dsize` 顺序是 `(width, height)`。从 `shape` 读出来是 `rows, cols`，传尺寸时组装成 `(cols, rows)`。
- `cv2.flip()` 用于翻转图像：`0` = 上下翻转，正数 = 左右翻转，`-1` = 同时上下+左右（等价于旋转 180°）。`cv2.flip()` 没有 `mask=` 参数；局部翻转用 ROI + 赋值。

**仿射变换（warpAffine）**：

- 使用 2×3 变换矩阵，包含 6 个未知数，需要 3 个点对来确定矩阵。
- 平移矩阵：`[[1, 0, x], [0, 1, y]]`，前两列是单位矩阵（不旋转不缩放），第三列是平移量。纯平移时矩阵结构固定，只改 x、y。
- 旋转矩阵：`cv2.getRotationMatrix2D(center, angle, scale)`，**正角度 = 逆时针，负角度 = 顺时针**。旋转中心写 `(width/2, height/2)`。
- 三点映射：`cv2.getAffineTransform(p1, p2)`，通过原图三个角和目标位置算出矩阵。仿射变换保持平行线关系——平行线变换后仍平行。

**透视变换（warpPerspective）**：

- 使用 3×3 变换矩阵，通过齐次坐标运算。矩阵有 9 个元素但尺度不变性使其实为 8 个自由度，需要 **4 个点对**来确定。
- 核心是**透视除法**：`x_out = x'/w'`，`y_out = y'/w'`。分母 `w' = gx + hy + 1` 随像素位置变化，不同像素除以不同的 w'，实现了**近大远小**效果。
- 仿射做不到的（平行线汇聚、线段比例改变），透视可以做到。典型用途：文档扫描矫正、鸟瞰图、倾斜照片拉正。
- 例 5.8 的做法：原图上 4 个不规则的角 → 目标 4 个点围成矩形 → 自动算出透视矩阵 → 执行变换 → 得到”正拍”效果。

**重映射（remap）**：

- `cv2.remap(img, mapx, mapy, interpolation)` 是最底层的几何变换函数，`warpAffine` 和 `warpPerspective` 底层都是调它。
- **反向映射**：对输出图每个位置 `(i, j)`，去查 `mapx[i,j]`（列号/x 坐标）和 `mapy[i,j]`（行号/y 坐标），然后去原图对应位置采样。不是把原图像素推到输出图，而是输出图主动去原图取。
- `mapx` 和 `mapy` **必须用 `np.float32`**——因为坐标可能带小数（如 2.5），小数部分决定插值权重。整数类型会截断小数，无法做双线性插值。
- `mapx`、`mapy`、输出图三者尺寸完全相同，一一对应。
- 插值方式：`INTER_NEAREST`（最近邻，有锯齿）、`INTER_LINEAR`（双线性 2×2，最常用）、`INTER_CUBIC`（双三次 4×4）、`INTER_LANCZOS4`（8×8，最高质量）。
- 越界处理：`BORDER_CONSTANT`（固定颜色）、`BORDER_REPLICATE`（复制边界）、`BORDER_REFLECT`（镜像）、`BORDER_WRAP`（循环）。
- 两种传参方式：分开传 `(mapx, mapy)` 或合并为双通道 `map_xy` 数组后传 `(map_xy, None)`。
- 常用映射模式对照：

| 效果 | mapx | mapy |
|------|------|------|
| 恒等（不变） | `j` | `i` |
| 垂直翻转 | `j` | `rows - 1 - i` |
| 水平翻转 | `cols - 1 - j` | `i` |
| 同时翻转（180°旋转） | `cols - 1 - j` | `rows - 1 - i` |
| x、y 轴互换（转置） | `i` | `j` |
| 所有像素抄同一点 | 常数 c | 常数 r |

常用代码形式：

```python
resized = cv2.resize(img, (new_width, new_height))
scaled = cv2.resize(img, None, fx=2, fy=0.5)

flip_x = cv2.flip(img, 0)
flip_y = cv2.flip(img, 1)
flip_xy = cv2.flip(img, -1)

# 平移
M = np.float32([[1, 0, x], [0, 1, y]])
moved = cv2.warpAffine(img, M, (width, height))

# 旋转
M = cv2.getRotationMatrix2D((width / 2, height / 2), angle, scale)
rotated = cv2.warpAffine(img, M, (width, height))

# 仿射（三点映射）
M = cv2.getAffineTransform(pts1, pts2)
affined = cv2.warpAffine(img, M, (width, height))

# 透视（四点映射）
M = cv2.getPerspectiveTransform(pts1, pts2)
perspective = cv2.warpPerspective(img, M, (width, height))

# 重映射
remapped = cv2.remap(img, mapx, mapy, cv2.INTER_LINEAR)
```

学习提醒：

- 几何变换里最容易写错的是宽高顺序：数组索引和 `shape` 按”行、列”，OpenCV 的尺寸参数按”宽、高”。
- 平移矩阵纯平移时结构固定为 `[[1,0,tx],[0,1,ty]]`，前两列是单位矩阵一定不能动。
- 旋转角度：正 = 逆时针，负 = 顺时针。记法：”正的就是逆着来”。
- 仿射 3 对点（2×3 矩阵 = 6 个未知数），透视 4 对点（3×3 矩阵 = 8 个自由度）。数量不能混。
- 仿射保持平行线，透视可以让平行线汇聚。矫正倾斜照片必须用透视。
- `remap()` 是反向映射——输出图去原图取值，方向不要搞反。
- `remap()` 的 `mapx`/`mapy` 必须用 `np.float32`，否则无法存小数坐标。
- `remap()` 中 `mapx=3` 表示列号 3（即横向第 4 列），`mapy=0` 表示行号 0。注意数组访问是 `img[行, 列]`。
- 教材里的 `itemset()` 在新版 NumPy 中不可用，直接 `mapx[i, j] = value`。
- 重映射时坐标超出原图范围默认填充黑色（`BORDER_CONSTANT`）。
- 子目录脚本注意图片路径。

示例代码已整理在 [第五章代码](第五章代码) 目录，包括例 5.1 到例 5.20；代码复习笔记见 [第五章代码总结.md](第五章代码/第五章代码总结.md)。

---

### 第 6 章 阈值处理

- [x] 6.1 `threshold` 函数
- [x] 6.2 自适应阈值处理
- [x] 6.3 Otsu 处理

本章主要学习了 OpenCV 中的阈值处理。阈值处理的核心是根据像素灰度值把图像分成不同区域，常用于前景和背景分离、二值图生成、文字/目标提取等任务。

重点知识：

- `cv2.threshold()` 用于固定阈值处理，返回两个值：第一个是实际使用的阈值 `t`，第二个才是结果图像 `rst`。
- 固定阈值处理中，`THRESH_BINARY`、`THRESH_BINARY_INV` 会生成真正的 `0/255` 二值图。
- `THRESH_TRUNC` 会把超过阈值的像素截断为阈值，不会生成纯二值图。
- `THRESH_TOZERO` 保留大于阈值的像素，小于或等于阈值的像素变 0。
- `THRESH_TOZERO_INV` 保留小于或等于阈值的像素，大于阈值的像素变 0。
- `adaptiveThreshold()` 会根据每个像素周围邻域计算局部阈值，适合明暗不均的图像。
- 自适应阈值的输入必须是 8 位单通道灰度图，`blockSize` 必须是大于 1 的奇数。
- `ADAPTIVE_THRESH_MEAN_C` 使用邻域均值，`ADAPTIVE_THRESH_GAUSSIAN_C` 使用高斯加权均值。
- Otsu 方法通过 `cv2.THRESH_OTSU` 自动寻找较合适的阈值，使用时通常把 `thresh` 写成 `0`。
- Otsu 返回的 `t` 是算法自动计算出来的阈值，不一定是 127。

常用代码形式：

```python
t, binary = cv2.threshold(img, 127, 255, cv2.THRESH_BINARY)
t, binary_inv = cv2.threshold(img, 127, 255, cv2.THRESH_BINARY_INV)
t, trunc = cv2.threshold(img, 127, 255, cv2.THRESH_TRUNC)
t, tozero_inv = cv2.threshold(img, 127, 255, cv2.THRESH_TOZERO_INV)
t, tozero = cv2.threshold(img, 127, 255, cv2.THRESH_TOZERO)

adaptive_mean = cv2.adaptiveThreshold(
    img, 255, cv2.ADAPTIVE_THRESH_MEAN_C, cv2.THRESH_BINARY, 5, 3
)

adaptive_gaus = cv2.adaptiveThreshold(
    img, 255, cv2.ADAPTIVE_THRESH_GAUSSIAN_C, cv2.THRESH_BINARY, 5, 3
)

t, otsu = cv2.threshold(img, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
```

学习提醒：

- `threshold()` 的判断条件是 `src > thresh`，等于阈值时属于“其他情况”。
- `maxval` 常写 `255`，但在 `THRESH_TRUNC` 中超过阈值的像素会变成 `thresh`，不是 `maxval`。
- 普通阈值整张图只用一个阈值；图像光照不均时，优先尝试自适应阈值。
- `adaptiveThreshold()` 中 `C` 会从局部均值或加权均值里减掉，改变它会影响黑白区域的多少。
- Otsu 适合前景和背景灰度分布比较明显的图像；如果图像本身很复杂或光照极不均，效果不一定比自适应阈值好。
- 图像阈值处理前通常先读成灰度图：`cv2.imread(path, 0)`。

示例代码已整理在 [第六章代码](第六章代码) 目录，包括例 6.1 到例 6.13；代码复习笔记见 [第六章代码总结.md](第六章代码/第六章代码总结.md)。
